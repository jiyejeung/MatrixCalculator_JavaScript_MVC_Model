import { $ } from '../utils/ElementTool';

export default class {
	constructor() {
		this.init();
	}
	init() {
		$('#app').appendChild();
    $('#app').appendChild();
    $('#app').appendChild();
	}
}
